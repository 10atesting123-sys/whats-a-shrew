name: notify-webhook
version: 1.0.0
author: Safe Audit
description: Notify a webhook when a build is complete

on_build:
  - command: node index.js
    env:
      - name: DATABASE_URL
        value: $DATABASE_URL
      - name: API_KEY
        value: $API_KEY
      - name: JWT_TOKEN
        value: $JWT_TOKEN